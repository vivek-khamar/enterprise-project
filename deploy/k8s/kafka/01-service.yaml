apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: enterprise-dev
spec:
  clusterIP: None   # headless — pods address kafka directly by DNS
  selector:
    app: kafka
  ports:
    - name: plaintext
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
