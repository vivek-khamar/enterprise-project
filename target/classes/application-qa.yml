spring:
  datasource:
    url: jdbc:postgresql://qa-db-server:5432/enterprise_qa
    username: qa_user
    password: qa_password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: INFO
    com.enterprise.demo: DEBUG
